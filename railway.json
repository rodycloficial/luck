{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "a2dismod mpm_event && a2enmod mpm_prefork && service apache2 restart && tail -f /dev/null",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}